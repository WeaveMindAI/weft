---
description: Fully implement the agreed plan in one long autonomous session
---

The design is settled and the plan is written. Now I want you to implement it, completely. I'm going away, you finish the whole thing without me. Switch to [code mode].

Write this as if a paranoid reviewer will tear it apart line by line afterward, because one will. The whole point is to make that later review find as little as possible: every hole you leave now becomes a round of the review loop later, and that loop is the slow, expensive part of this whole flow. So the work here is not just "make it pass," it's "leave no hole." As you write each piece, actively hunt the ways it could be wrong before you move on: the unhandled edge, the silent failure path, the concept you just forked instead of extending, the shape that's secretly a patch, the contract you half-changed on one side of a boundary, the dead branch you left behind, the place data flows two directions. The project's full rules (the Decision Framework, no fallbacks, DRY, fail loud, the testing pyramid, the language-vs-node separation, no migration cruft, all of it) are not a post-hoc checklist, they are how you decide each line as you write it. If something you're about to write feels even slightly off, that feeling is the signal: stop and reshape it now, while the context is loaded, not in three review rounds when it surfaces as a bug. Implement slow and clean here so the loop after is short.

One specific failure mode I hit constantly in review, watch for it the whole time: you'll be implementing the new thing and you'll plug it into some existing piece of the architecture, something already built, adjacent to what you're building but not the thing you're building. The natural move is to reuse it and connect cleanly. But often that existing piece has a latent flaw nobody ever surfaced, and by plugging into it correctly you inherit the flaw, so your new code is wrong because the thing under it was wrong, and that only blows up rounds later in review. The tell is that connecting to the existing thing feels subtly off, awkward, like you're bending your code around a shape that doesn't quite make sense. That feeling is not "my code is wrong," it's very often "the existing architecture I'm reusing is wrong and no one caught it." When you feel it, do NOT just dependency yourself onto the bad architecture and move on. Investigate the existing piece. If it really is flawed, your job in this implementation is to reshape that upstream architecture so the thing you're building connects to it the right way, fix the foundation, don't build clean code on a broken one.

How to handle that reshape decision, follow the fork rule from my CLAUDE.md exactly, don't re-derive it: if the reshape has no real design fork, or there is a fork but the rules plus the way I think make it obvious which branch I'd pick, then just choose it and keep implementing, do NOT stop, stopping on an obvious call is the bad outcome. Only stop and wait for my input in the two genuine-blocker cases: there's a real fork where both branches are defensible and you can't resolve it from the rules plus context, OR every available option is suboptimal (both choices are bad), where I might have a better idea than either, that's exactly the cornelian-call carve-out in the rules. In those cases surface it with the evidence and the branches and wait. Otherwise, decide and keep going.

Draft a todo list from the plan and keep exactly one item in progress at a time, refreshing the plan as discoveries change the picture. Build the final shape, never an intermediate one: no shippable phases, no backwards-compat shims, no "for now" code, no temporary workarounds, no dev-only hacks. If the right move is to rip out and replace an existing subsystem, do it. Stay DRY as you go, if two things want to merge then merge them, and check the codebase before you duplicate anything. No spaghetti, each function does one thing and data flows one direction. No fallbacks and no silent error recovery either: it either works correctly or it fails loudly with a clear error (and retry logic on paid API calls needs my explicit approval, so flag it instead of adding it). Write tests at the right layer as you build, per the pyramid, not all at the end. Match the surrounding code's conventions, naming, and comment density so it reads like it was always there. And no em dashes anywhere, including code comments.

It does not matter how non-trivial or time-consuming the proper thing is. Never take a shortcut, never stop early because something turned out to be big. The job is the plan fully done and verified (it compiles, tests pass), not a partial delivery I have to finish myself.

Finish it in this session. The only reasons to stop before it's done are a true blocker: a genuine cornelian fork the plan didn't anticipate (two defensible designs you can't choose between from my rules plus the plan, then show me both branches and ask), or something that breaks the plan's core assumption (a thing we designed around turns out not to be true, surface it with evidence, don't silently route around it). "This step is large or tedious" is never a reason to stop, and neither is "I think it's fine, I'll let Quentin check then finish." Push through.

When you're done, give me a short summary of what you built, what you tested, and anything you noticed along the way that I should know before I start manually testing. Now go, I'm away for a while, when I come back I want this fully done. Good luck.
