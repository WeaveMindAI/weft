{
  "type": "OpenRouterInference",
  "label": "OpenRouter",
  "description": "AI language model completion through OpenRouter. The credential decision comes from a wired OpenRouterAccess node (Credits or your own key); model, system prompt, and sampling parameters come from a wired OpenRouterConfig node, and without one the defaults apply (default model: anthropic/claude-sonnet-4.6).",
  "category": "AI",
  "tags": [
    "ai",
    "gpt",
    "claude",
    "completion",
    "chat",
    "generate"
  ],
  "icon": "BrainCircuit",
  "color": "#7c6f9f",
  "inputs": [
    {
      "name": "prompt",
      "type": "String",
      "required": true,
      "description": "The prompt to send to the LLM"
    },
    {
      "name": "account",
      "type": "Access",
      "required": true,
      "description": "The OpenRouter access (wire an OpenRouterAccess node's output): whose key the call spends."
    },
    {
      "name": "config",
      "type": "Dict[String, String | Number | Boolean]",
      "required": false,
      "description": "Optional OpenRouterConfig node carrying ALL settings (model, system prompt, parameters, key). Unwired, the defaults apply.",
      "exposure": "wire"
    },
    {
      "name": "parseJson",
      "type": "Boolean",
      "exposure": "config",
      "default": false,
      "label": "Parse JSON",
      "description": "Parse and repair JSON from the response (changes output to Dict)."
    }
  ],
  "outputs": [
    {
      "name": "response",
      "type": "MustOverride",
      "required": false,
      "description": "LLM response. Declare type in Weft: String without parseJson, or Dict/JsonDict with parseJson. With parseJson, you can also add custom output ports to extract specific JSON keys directly."
    }
  ],
  "requires_infra": false,
  "features": {
    "canAddOutputPorts": true
  }
}
