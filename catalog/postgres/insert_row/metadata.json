{
  "type": "PostgresInsertRow",
  "label": "Postgres: Insert Row",
  "description": "Insert one row into a table, columns from a JSON object, and get the inserted row back (defaults and generated ids filled in).",
  "tags": [
    "postgres",
    "database",
    "insert"
  ],
  "icon": "DatabaseBackup",
  "color": "#336791",
  "inputs": [
    {
      "name": "account",
      "type": "Access", "accepts": ["wire"],
      "required": true,
      "requiresValues": [
        "host",
        "database",
        "user",
        "password"
      ],
      "description": "The database's connection (wire a PostgresAccess node, or a PostgresDatabase's access output)."
    },
    {
      "name": "table",
      "type": "String",
      "required": true,
      "description": "The table to insert into."
    },
    {
      "name": "values",
      "type": "JsonDict",
      "required": true,
      "description": "The row as column: value pairs, e.g. { \"email\": \"a@b.c\", \"age\": 30 }."
    }
  ],
  "outputs": [
    {
      "name": "row",
      "type": "JsonDict",
      "description": "The inserted row as the database stored it. The node fails rather than emitting nothing: if the table has a rule or trigger that discards the insert, no row was stored and there is nothing to pass on."
    }
  ]
}
