{
  "type": "ApiPost",
  "label": "API Endpoint (POST)",
  "description": "Start an execution when an HTTP POST request arrives at a project-specific URL. Declare the expected body fields as output ports; the node extracts each from the request body at fire time.",
  "category": "Triggers",
  "tags": ["webhook", "http", "trigger", "api"],
  "icon": "Webhook",
  "color": "#22c55e",
  "inputs": [],
  "outputs": [
    { "name": "receivedAt", "type": "String", "required": false, "description": "ISO-8601 timestamp of when the request arrived." }
  ],
  "fields": [
    {
      "key": "apiKey",
      "label": "API Key",
      "field_type": { "kind": "password" },
      "required": false,
      "description": "Optional. If set, the incoming request must include this key in the X-API-Key header."
    }
  ],
  "entry": [
    {
      "kind": "webhook",
      "path": "",
      "auth": { "kind": "optional_api_key", "field": "apiKey" }
    }
  ],
  "requires_infra": false,
  "features": {
    "isTrigger": true,
    "triggerCategory": "Webhook",
    "canAddOutputPorts": true
  }
}
