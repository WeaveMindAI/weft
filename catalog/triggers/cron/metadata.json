{
  "type": "Cron",
  "label": "Cron Schedule",
  "description": "Fire on a schedule. Standard cron expression in the `cron` field.",
  "category": "Triggers",
  "tags": ["cron", "schedule", "timer", "trigger"],
  "icon": "Clock",
  "color": "#22c55e",
  "inputs": [],
  "outputs": [
    { "name": "scheduledTime", "type": "String", "required": false, "description": "ISO-8601 timestamp of the scheduled fire time" },
    { "name": "actualTime", "type": "String", "required": false, "description": "ISO-8601 timestamp of when the firing actually happened" }
  ],
  "fields": [
    {
      "key": "cron",
      "label": "Cron",
      "field_type": { "kind": "text" },
      "required": true,
      "default_value": "*/5 * * * *",
      "description": "Standard cron expression. Default fires every 5 minutes."
    }
  ],
  "entrySignals": [
    { "kind": "timer", "is_resume": false }
  ],
  "requires_infra": false,
  "features": {
    "isTrigger": true,
    "triggerCategory": "Schedule"
  }
}
