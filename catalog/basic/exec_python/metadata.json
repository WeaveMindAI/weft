{
  "type": "ExecPython",
  "label": "Python",
  "description": "Run a Python snippet. Declare the node's inputs and outputs inline: `ExecPython(x: Number) -> (result: String) { code: \"...\" }`. Inputs arrive as Python variables named after each port. The Python code returns a dict keyed by output port name; a key set to None (or missing) emits no pulse on that port. Exceptions fail the node with the full traceback.",
  "category": "basic",
  "tags": ["python", "script", "custom"],
  "icon": "Code2",
  "color": "#3b82f6",
  "inputs": [],
  "outputs": [],
  "fields": [
    {
      "key": "code",
      "label": "Python code",
      "field_type": { "kind": "textarea" },
      "required": true
    }
  ],
  "entry": [],
  "requires_infra": false,
  "features": {
    "canAddInputPorts": true,
    "canAddOutputPorts": true
  }
}
