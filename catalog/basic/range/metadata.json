{
  "type": "Range",
  "label": "Range",
  "description": "Produce a list of numbers between `from` (inclusive) and `to` (exclusive), stepping by `step`. Commonly used to drive `Loop(over: [\"values\"])` for count-based iteration.",
  "tags": ["number", "list", "range", "loop"],
  "icon": "Hash",
  "color": "#64748b",
  "inputs": [
    { "name": "from", "type": "Number", "required": false, "default": 0 },
    { "name": "to", "type": "Number", "required": true },
    { "name": "step", "type": "Number", "required": false, "default": 1 }
  ],
  "outputs": [
    { "name": "values", "type": "List[Number]", "required": false }
  ],
  "requires_infra": false
}
