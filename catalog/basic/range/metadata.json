{
  "type": "Range",
  "label": "Range",
  "description": "Yield the numbers between `from` (inclusive) and `to` (exclusive) one at a time, stepping by `step`. Commonly used to drive `Loop(over: [\"values\"])` for count-based iteration; the loop pulls one number per iteration, so large ranges never sit in memory.",
  "tags": ["number", "stream", "range", "loop"],
  "icon": "Hash",
  "color": "#64748b",
  "inputs": [
    { "name": "from", "type": "Number", "required": false, "default": 0 },
    { "name": "to", "type": "Number", "required": true },
    { "name": "step", "type": "Number", "required": false, "default": 1 }
  ],
  "outputs": [
    { "name": "values", "type": "Generator[Number]", "required": false }
  ],
  "requires_infra": false
}
