# Extra dependencies this node needs beyond the codegen base
# (weft-core, tokio, serde, serde_json, async-trait, anyhow, tracing,
# uuid). Base deps are always available; list only the extras here.
[dependencies]
