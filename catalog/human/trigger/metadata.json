{
  "type": "HumanTrigger",
  "label": "Human Trigger",
  "description": "Start an execution when a human submits a form. Form fields determine the output ports.",
  "tags": [
    "human",
    "form",
    "trigger",
    "manual"
  ],
  "icon": "User",
  "color": "#22c55e",
  "inputs": [
    {
      "name": "title",
      "type": "String",
      "widget": {
        "kind": "text"
      },
      "label": "Title",
      "placeholder": "Submit",
      "description": "Title the user sees at the top of the form."
    },
    {
      "name": "description",
      "type": "String",
      "widget": { "kind": "textarea" },
      "label": "Description",
      "description": "Description shown under the title."
    },
    {
      "name": "fields",
      "type": "List[JsonDict]",
      "required": true,
      "widget": {
        "kind": "entry_list"
      },
      "label": "Form fields",
      "description": "Each field contributes an output port per its kind (see the node's portsFromConfig)."
    }
  ],
  "outputs": [],
  "requires_infra": false,
  "features": {
    "isTrigger": true
  },
  "validate": [
    {
      "when": {
        "kind": "not",
        "of": {
          "kind": "config_nonempty",
          "field": "fields"
        }
      },
      "then": {
        "message": "HumanTrigger '{id}' has no form fields; the user would see an empty form.",
        "level": "structural",
        "field": "fields"
      }
    }
  ]
}
