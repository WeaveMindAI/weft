[package]
name = "weft-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
async-trait = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }
tokio = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
inventory = { workspace = true }
cron = "0.12"
sha2 = { workspace = true }

[dev-dependencies]
# Used by the `stress_test!` macro (test-only). Provides
# `FutureExt::catch_unwind` so a panic in any stress iteration is
# captured and reported as a clean test failure with the iteration
# index, rather than poisoning the runtime.
futures = { workspace = true }
