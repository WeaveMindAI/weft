# The simplest program: a literal Text flows into a Debug. A plain `weft run`
# fires it; the rig asserts the Debug node received the literal value.

greeting = Text { value: "hello weft" }
out = Debug

out.data = greeting.value
