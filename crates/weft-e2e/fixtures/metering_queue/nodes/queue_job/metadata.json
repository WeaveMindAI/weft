{
  "type": "QueueJob",
  "label": "Queue job",
  "description": "Submit a job to a queue-style paid provider and, optionally, read its answer back. Used by the e2e rig to prove what the cost trail records when a submitted job is never read: money out with no figure, never a zero and never nothing.",
  "tags": ["e2e", "metering", "queue"],
  "icon": "ListOrdered",
  "color": "#7c6f9f",
  "inputs": [
    {
      "name": "readBack",
      "type": "Boolean",
      "widget": { "kind": "checkbox" },
      "default": false,
      "label": "Read the answer back",
      "description": "Off: submit and walk away, so nothing ever states what the job cost."
    },
    {
      "name": "connection",
      "type": "Access",
      "widget": { "kind": "access" },
      "label": "Connection",
      "description": "The queue provider's connection. Its stored base points at the rig's fake."
    }
  ],
  "outputs": [
    { "name": "requestId", "type": "String" }
  ],
  "requires_infra": false,
  "service": {
    "service": "queue_fake",
    "label": "Queue fake (e2e)",
    "acquisition": {
      "kind": "static",
      "fields": [
        { "name": "key", "label": "API key", "placeholder": "anything" },
        { "name": "base", "label": "Base URL", "placeholder": "http://host:port" }
      ]
    },
    "auth": [
      { "kind": "header", "name": "Authorization", "value": "Bearer {key}" },
      { "kind": "base_url", "value": "{base}" }
    ]
  }
}
